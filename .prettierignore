**/pnpm-lock.yaml
**/fixtures/openai-weather-response.json
**/fixtures/openai-stream-response.json
scripts/
**/notes/**
**/.cache/**
**/.esm-cache/**
**/dist/**
**/coverage/**
**/.react-router/
**/.netlify/
**/build/**

# Eval harness output (transient generated apps)
eval/codegen-edit/archive-*/

# Eval result App snapshots can intentionally capture broken/truncated JSX.
vibes/eval/**/App.jsx

# Generated demo/example vibe source (prompt-generated; style-exempt per
# agents/rules-bag.md, same as the eval snapshots above). Prettier must not
# reflow generated output — it just re-drifts on regeneration (#2775).
vibes/*/App.jsx
vibes/*/access.js

# LLM prompt files — filename-line + fence must be adjacent (no blank line)
prompts/pkg/system-prompt.md
prompts/pkg/system-prompt-initial.md
prompts/pkg/llms/

# Migrated good.vibes.diy site (#2849). Mostly prompt-generated example-app
# source (App.jsx/access.js, same style-exempt rationale as vibes/* above) plus
# hand-tuned .hbs landing pages whose inline CSS prettier must not reflow.
# Exempt the whole subdir for now; the SSG's own source is kept tidy by hand.
landing-pages/

# License files — canonical legal text must stay byte-exact for GitHub's
# license detection (licensee); prettier must not reflow them.
LICENSE.md
**/LICENSE.md
