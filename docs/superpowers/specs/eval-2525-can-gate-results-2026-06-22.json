{
  "meta": {
    "issue": "https://github.com/VibesDIY/vibes.diy/issues/2525",
    "title": "A→B gate for the useVibe().can prompt flip (generation QA)",
    "date": "2026-06-22",
    "tool": "vibes-diy@2.5.13",
    "handle": "garden-gnome",
    "corpus": "vibes/eval/can-gate-2525/",
    "method": "Generate one vibe per matrix row with `vibes-diy generate`, pull App.jsx + access.js, grade against the can-flip rule. unknown-rate determined by static analysis of the client runner (vibes.diy/vibe/runtime/access-runner.ts): unknown fires only on async fns, non-extractable exports (named arrow-const), compile errors, or unimplemented ctx members — none present here.",
    "gradeKey": {
      "PASS": "write surfaces gate on can.*, identity via useViewer only, no hidden-form regression, no unknown",
      "SOFT-FAIL": "works and gates on can.*, but a minor anti-pattern or a missed prompt dimension",
      "FAIL": "wrong gating, a hidden/broken write surface, or an unknown verdict"
    }
  },
  "results": [
    {
      "id": "recipe-journal",
      "matrixRow": 1,
      "dimension": "single-author / owner",
      "prompt": "A personal recipe journal where I write down recipes I cook, with ingredients and steps.",
      "url": "https://vibes.diy/vibe/garden-gnome/eval2525-recipe-journal",
      "dbName": "recipeJournal",
      "accessExport": "export function recipeJournal(doc, oldDoc, user, ctx)",
      "grade": "PASS",
      "reason": "Owner-only writes gated via can.create({type:'recipe', createdBy}); per-row edit/delete on can.edit/can.delete; ViewerTag for identity. Extractable named export matching db.",
      "createGatedOnCan": true,
      "editDeleteGatedOnCan": true,
      "rendersReason": false,
      "ownerWriteGatedOnCan": true,
      "viewerOnlyForIdentity": true,
      "unknown": false,
      "nits": ["Denied compose form is hidden with no reason fallback — anon sees nothing instead of a sign-in prompt."]
    },
    {
      "id": "team-board",
      "matrixRow": 2,
      "dimension": "channel membership",
      "prompt": "A team message board with channels, where people post messages into different channels.",
      "url": "https://vibes.diy/vibe/garden-gnome/eval2525-team-board",
      "dbName": "board",
      "accessExport": "export function board(doc, oldDoc, user, ctx)",
      "grade": "FAIL",
      "reason": "App.jsx wiring is correct (channel create on can.create({type:'channel'}) not isOwner; message compose on can.create({type:'message', channelId, authorHandle}) with reason; isViewerPending skeleton). But the generated access.js is self-contradictory: the channel doc grants the channel `public` READ (grant:{public:[id]}) while a message requires ctx.requireAccess(channelId), which checks effective user/role channel membership (grants.channels), NOT public read (grants.publicChannels). A public grant never confers membership, so can.create({type:'message'}) returns `not in channel` for every non-admin user — no one but the owner-in-admin-mode can ever post. The primary write flow is broken (confirmed by Codex review and verified against grant-reduce.ts/who-am-i.ts + access-runner.ts requireAccess). This is a hidden-write regression caused by the access.js, faithfully reflected by can.*.",
      "createGatedOnCan": true,
      "editDeleteGatedOnCan": true,
      "rendersReason": true,
      "ownerWriteGatedOnCan": true,
      "viewerOnlyForIdentity": true,
      "unknown": false,
      "writeFlowBroken": true,
      "nits": [
        "access.js grants public READ but gates writes on ctx.requireAccess(channel) — public read does not satisfy requireAccess, so non-owner posts are denied."
      ]
    },
    {
      "id": "forum-mods",
      "matrixRow": 3,
      "dimension": "owner-managed roles + role-gated posts",
      "prompt": "A community forum with discussion posts, where the owner can appoint moderators who help manage the posts.",
      "url": "(local only; first push errored server-side, re-generated as eval2525-forum-mods2 then deployed)",
      "dbName": "forum",
      "accessExport": "export function forum(doc, oldDoc, user, ctx)",
      "grade": "PASS",
      "reason": "BEST EXAMPLE of the target pattern. Role grant gated on can.create({type:'modGrant', userHandle}) — NOT isOwner. Posts on can.create({type:'post', authorHandle}) with reason. Mod actions on can.create({type:'modAction', byHandle}). access.js uses ctx.requireRole('moderator') (implemented in the client runner). No isOwner write-gate anywhere.",
      "createGatedOnCan": true,
      "editDeleteGatedOnCan": true,
      "rendersReason": true,
      "ownerWriteGatedOnCan": true,
      "viewerOnlyForIdentity": true,
      "unknown": false,
      "nits": [
        "First generation truncated mid-stream and the server push failed; needed a re-generation (infra flakiness, not a model-quality issue)."
      ]
    },
    {
      "id": "guestbook",
      "matrixRow": 4,
      "dimension": "anonymous writes",
      "prompt": "A public guestbook anyone can sign with their name and a short message.",
      "url": "https://vibes.diy/vibe/garden-gnome/eval2525-guestbook",
      "dbName": "guestbook",
      "accessExport": "export function guestbook(doc, oldDoc, user, ctx)",
      "grade": "SOFT-FAIL",
      "reason": "can.create wiring and reason rendering are clean and correct. But the prompt says 'anyone can sign' and access.js requires sign-in (if (!user) throw) instead of returning allowAnonymous:true — so an anonymous visitor cannot sign; can.create returns {ok:false, reason:'sign in...'} for anon. The anonymous-write dimension is not satisfied.",
      "createGatedOnCan": true,
      "editDeleteGatedOnCan": false,
      "rendersReason": true,
      "ownerWriteGatedOnCan": true,
      "viewerOnlyForIdentity": true,
      "unknown": false,
      "nits": [
        "Model defaulted to login-required even though the prompt explicitly invites anonymous writes; should emit allowAnonymous:true."
      ]
    },
    {
      "id": "photo-wall",
      "matrixRow": 5,
      "dimension": "comments (canonical case)",
      "prompt": "A photo wall where people post photos and comment on each other's posts.",
      "url": "https://vibes.diy/vibe/garden-gnome/eval2525-photo-wall",
      "dbName": "snapshotWall",
      "accessExport": "export function snapshotWall(doc, oldDoc, user, ctx)",
      "grade": "FAIL",
      "reason": "App.jsx gates posts and comments on can.create with proper candidates (authorHandle, channelId) and renders reason — the wiring looks right. But the same public-read-vs-membership bug as team-board breaks it: posts/comments require ctx.requireAccess('ch:wall'), while ch:wall is only granted public READ (grant:{public:['ch:wall']}). requireAccess checks grants.channels (effective membership), not grants.publicChannels, so can.create returns `not in channel` for every non-admin — the canonical comment flow is broken for all non-owner visitors. My earlier 'canonical comment case is correct' was wrong (caught by Codex review). Secondary issue: the channel-bootstrap write is gated on useViewer().isOwner instead of can.create({type:'channel'}).",
      "createGatedOnCan": true,
      "editDeleteGatedOnCan": false,
      "rendersReason": true,
      "ownerWriteGatedOnCan": false,
      "viewerOnlyForIdentity": false,
      "unknown": false,
      "writeFlowBroken": true,
      "nits": [
        "access.js grants ch:wall public READ but gates post/comment writes on ctx.requireAccess('ch:wall') — non-owner posts and comments are denied.",
        "Channel-bootstrap effect gated on isOwner instead of can.create({type:'channel'})."
      ]
    },
    {
      "id": "trip-planner",
      "matrixRow": 6,
      "dimension": "multi-database",
      "prompt": "A shared trip planner with a day-by-day itinerary and a separate packing list.",
      "url": "https://vibes.diy/vibe/garden-gnome/eval2525-trip-planner",
      "dbName": "wayfare",
      "accessExport": "export default function (doc, oldDoc, user, ctx)",
      "grade": "PASS",
      "reason": "Every write surface gates on can.create/can.edit/can.delete with proper candidates; per-row delete and packing-checkbox edit gated. Default export covers the single db.",
      "createGatedOnCan": true,
      "editDeleteGatedOnCan": true,
      "rendersReason": false,
      "ownerWriteGatedOnCan": true,
      "viewerOnlyForIdentity": true,
      "unknown": false,
      "nits": [
        "Chose a single db ('wayfare') with doc types (trip/activity/packing) rather than the multi-database design the prompt suggested — valid, but the multi-db dimension was not exercised.",
        "No reason fallback rendered on denied surfaces (writes just hidden/disabled)."
      ]
    },
    {
      "id": "task-list",
      "matrixRow": 7,
      "dimension": "per-doc edit/delete",
      "prompt": "An editable shared task list where people add tasks and can edit or delete them, with checkboxes to mark done.",
      "url": "https://vibes.diy/vibe/garden-gnome/eval2525-task-list",
      "dbName": "coTask",
      "accessExport": "export default function (doc, oldDoc, user, ctx)",
      "grade": "FAIL",
      "reason": "Edit/delete are correctly gated on can.edit(task).ok / can.delete(task).ok. But the create surface (TaskComposer) is a non-functional stub that permanently renders 'Loading…' — no input, no can.create gate, no way to add a task. The primary write surface is broken. Deployed cleanly (not a truncation): the model emitted a placeholder it never filled.",
      "createGatedOnCan": false,
      "editDeleteGatedOnCan": true,
      "rendersReason": false,
      "ownerWriteGatedOnCan": true,
      "viewerOnlyForIdentity": true,
      "unknown": false,
      "nits": ["TaskComposer is a stub; the add-task form was never generated."]
    }
  ],
  "rollup": {
    "total": 7,
    "pass": 3,
    "softFail": 1,
    "fail": 3,
    "unknownCount": 0,
    "hiddenWriteMisgates": 2,
    "hiddenWriteMisgatesNote": "team-board and photo-wall. NOT caused by a wrong can verdict or a malformed candidate — caused by a self-contradictory generated access.js (grants a channel public READ, then gates writes on ctx.requireAccess(thatChannel), which requires effective membership that public read never confers). can.* faithfully reflects the buggy access.js. Surfaced by Codex review; verified against grant-reduce.ts, who-am-i.ts, and access-runner.ts.",
    "correctionNote": "Initial grading (4 PASS / 2 SOFT-FAIL / 1 FAIL, 0 hidden-write mis-gates) missed the public-read-vs-requireAccess-membership bug in team-board (was PASS) and photo-wall (was SOFT-FAIL). Both corrected to FAIL after Codex review.",
    "commonAntiPatterns": [
      "PUBLIC-READ vs WRITE-MEMBERSHIP CONFUSION (most important): access fn grants a channel `grant:{public:[ch]}` (read) and then gates writes on ctx.requireAccess(ch). requireAccess checks effective membership (grants.channels), not public read (grants.publicChannels), so non-owner writes are silently denied. The fix the model needs: either don't requireAccess for an open public channel, or grant write-membership explicitly (grant:{users|roles}) / use a public-write descriptor. Affected team-board and photo-wall (2/7).",
      "No reason fallback on denied write surfaces — the form is silently hidden instead of rendering can.*().reason as sign-in/join copy (recipe-journal, trip-planner).",
      "Owner-bootstrap/management write gated on useViewer().isOwner instead of can.create({type:'channel'}) (photo-wall).",
      "Anonymous-write prompt produced a login-required access fn instead of allowAnonymous:true (guestbook).",
      "Generation-stream reliability: 2 of 8 generation attempts truncated mid-stream; one failed the server push entirely and needed a re-gen; another shipped a stubbed composer. Infra signal, not can-flip, but it threatens the eval."
    ],
    "verdict": "DO NOT FLIP YET",
    "verdictRationale": "The can-flip HOOK is doing its job — App.jsx gates every write on can.* with proper candidates, useViewer is identity-only in 5/7, unknown rate is 0, and owner-management is correctly routed through can.* (forum is a textbook example). But the eval surfaced a real generation-quality bug in the access.js the hook depends on: the model conflates public READ with channel write-MEMBERSHIP, emitting grant:{public:[ch]} + ctx.requireAccess(ch), which denies all non-owner writes. That is exactly the 'hidden write form' bug class this effort targets — present in 2/7 — so it fails pass-criterion #2. The flip is reversible and we should stay at A. Prompt tweak-spots before re-running: (1) PRIMARY — teach that ctx.requireAccess gates on membership, so a public channel anyone can post to must not be behind requireAccess (or must grant write-membership explicitly); (2) always render can.*().reason on denial; (3) teach allowAnonymous for 'anyone can' prompts; (4) steer owner-bootstrap writes through can.create. Re-run the matrix after (1); I'd expect team-board + photo-wall to recover."
  }
}
