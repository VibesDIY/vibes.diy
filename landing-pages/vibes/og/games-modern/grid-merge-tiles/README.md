# grid-merge-tiles

> Swipe-merge on a 4x4 grid: tiles slide on swipe, matching tiles merge and double in value (2 to 4 to 8 ...). Game ends when no moves remain. Score saved per game in Fireproof; daily best and lifetime best displayed.

Live at [https://vibes.diy/vibe/og/grid-merge-tiles](https://vibes.diy/vibe/og/grid-merge-tiles)

Single-file React app built with [vibes.diy](https://vibes.diy). Visit the live url to manage access.

## Run it

```sh
npx vibes-diy push     # uploads App.jsx, prints a live HTTPS URL
```

Edit [App.jsx](App.jsx) and push again to iterate.

## Commands

- `npx vibes-diy push` — deploy the current directory
- `npx vibes-diy push --instant-join` — deploy with auto-accept sharing
- `npx vibes-diy generate "prompt"` — generate a new app from a prompt
- `npx vibes-diy help` — full command list
