# Harvest Files

One JSON per source. Schema matches `../../research/harvest/` (same pipeline).

Each file: `source`, `location`, `region`, `fetched`, `items[]` with `title`, `department`, `year`, `url`, `fetch_method`, `topics[]`, `source_text`.
