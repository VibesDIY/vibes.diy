name: "create-vibe-publish"
description: "publish create-vibe to npm"
inputs:
  NPM_TOKEN:
    description: "NPM token for publishing packages"
    required: true
runs:
  using: "composite"
  steps:
    - name: publish
      shell: bash
      working-directory: create-vibe/pkg
      env:
        NPM_TOKEN: ${{ inputs.NPM_TOKEN }}
      run: |
        set -e
        rm -f **/.npmrc
        echo "GITHUB_REF->"$GITHUB_REF
        echo "//registry.npmjs.org/:_authToken=${{ inputs.NPM_TOKEN }}" > ~/.npmrc
        mkdir -p dist
        echo "//registry.npmjs.org/:_authToken=${{ inputs.NPM_TOKEN }}" > dist/.npmrc
        pnpm run publish --registry https://registry.npmjs.org/
